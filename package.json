{
  "dependencies": {
    "express": "^4.18.2",
    "nodemon": "^2.0.22",
    "openai": "^3.2.1",
    "prettier": "^2.8.7"
  }
  
  "scripts": {
    "start": "node server.js",
    "build": "npm run test && npm run compile",
    "test": "mocha",
    "compile": "babel src -d lib"
  }
}
